---
must_haves:
  - Add `loop={false}` to the first `<TextType>` component in `ScrollAnimation.tsx` relative to the "Operating Across All Dubai Authorities" tagline.
---

# Quick Task 7: Disable Loop on TextType

## Tasks

<tasks>

<task>
  <id>disable_loop</id>
  <description>Add loop={false} to tagline</description>
  <files>
    - src/components/ScrollAnimation.tsx
  </files>
  <action>
    Modify `TextType` for the tagline to include `loop={false}` to prevent it from continuously typing and deleting.
  </action>
  <verify>Check frontend that the tagline only types in once.</verify>
  <done>false</done>
</task>

</tasks>
